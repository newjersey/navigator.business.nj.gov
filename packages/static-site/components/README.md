# Components Folder

This folder contains presentational React components for the static site.

- Components focus on rendering and composition.
- Components should receive typed data through props instead of loading data directly.
- Landing page UI modules live in `components/landing`.

