# Messages Folder

This folder stores locale-specific message payloads for the static site.

- Each locale has a JSON file with translated labels and content.
- Message files are treated as data inputs to domain loaders and validators.
- Keep keys consistent across locale files so typed consumers stay stable.

