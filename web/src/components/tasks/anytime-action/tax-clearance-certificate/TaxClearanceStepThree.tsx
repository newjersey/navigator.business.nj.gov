import { ReactElement } from "react";

// interface Props {
//   setStepIndex: (x: number) => void;
// }
// export const TaxClearanceStepThree = (props: Props): ReactElement => {
export const TaxClearanceStepThree = (): ReactElement => {
  // const { Config } = useConfig();

  return (
    <>
      <div data-testid={"review-tab"}>review</div>
    </>
  );
};
