# pnpm (v11+) reads project configuration from `pnpm-workspace.yaml`, not
# from this file — see that file for `overrides`, `allowBuilds`, and peer
# dependency settings. This `.npmrc` is reserved for the settings pnpm still
# reads here: registry and auth configuration. None are needed today; keep
# this file for whenever one is.
